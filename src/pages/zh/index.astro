---
import BaseLayout from '../../layouts/BaseLayout.astro';
import providers from '../../data/providers.json';
import { t } from '../../i18n/utils';

const locale = 'zh';

// Sort by overall rank
const sortedProviders = [...providers].sort((a, b) => a.overallRank - b.overallRank);
const domesticProviders = sortedProviders.filter(p => p.category === 'domestic');
const intlProviders = sortedProviders.filter(p => p.category === 'international');
const aggregatorProviders = sortedProviders.filter(p => p.category === 'aggregator');

// JSON-LD: FAQPage for homepage
const faqJsonLd = {
  "@context": "https://schema.org",
  "@type": "FAQPage",
  "mainEntity": [
    {
      "@type": "Question",
      "name": t(locale, 'home.faq.q1'),
      "acceptedAnswer": {
        "@type": "Answer",
        "text": t(locale, 'home.faq.a1').replace(/<[^>]+>/g, '')
      }
    },
    {
      "@type": "Question",
      "name": t(locale, 'home.faq.q2'),
      "acceptedAnswer": {
        "@type": "Answer",
        "text": t(locale, 'home.faq.a2').replace(/<[^>]+>/g, '')
      }
    },
    {
      "@type": "Question",
      "name": t(locale, 'home.faq.q3'),
      "acceptedAnswer": {
        "@type": "Answer",
        "text": t(locale, 'home.faq.a3').replace(/<[^>]+>/g, '')
      }
    }
  ]
};
---

<BaseLayout
  title="AI API Token 价格对比与深度测评 | APIRank"
  description="APIRank：最全面的 AI API Token 价格对比网站。收录 OpenAI、Claude、DeepSeek、Google Gemini、阿里通义、百度文心、腾讯混元、字节豆包、智谱 GLM、月之暗面 Kimi 等主流 AI 厂商的最新价格、免费额度、国内可用性，帮你找到最划算的 AI API 方案。"
>
  <!-- JSON-LD FAQ -->
  <script type="application/ld+json" set:html={JSON.stringify(faqJsonLd)} />

  <!-- Hero Section -->
  <section class="bg-gradient-to-br from-blue-50 via-white to-indigo-50 py-16 sm:py-24">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center max-w-3xl mx-auto">
        <h1 class="text-4xl sm:text-5xl font-bold text-gray-900 mb-6 leading-tight">
          {t(locale, 'home.hero.title')}
          <span class="text-blue-600">{t(locale, 'home.hero.titleHighlight')}</span>
          {t(locale, 'home.hero.titleSuffix')}
        </h1>
        <p class="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
          {t(locale, 'home.hero.subtitle')}
        </p>
        <div class="flex flex-col sm:flex-row gap-3 justify-center">
          <a href="#providers" class="btn-primary text-lg py-3 px-8">
            {t(locale, 'home.hero.ctaCompare')}
          </a>
          <a href="/zh/tutorials" class="btn-secondary text-lg py-3 px-8">
            {t(locale, 'home.hero.ctaTutorials')}
          </a>
        </div>
      </div>
      
      <!-- Quick Stats -->
      <div class="mt-12 grid grid-cols-3 gap-4 sm:gap-8 max-w-2xl mx-auto text-center">
        <div>
          <div class="text-3xl sm:text-4xl font-bold text-blue-600">10+</div>
          <div class="text-sm text-gray-500 mt-1">{t(locale, 'home.stats.providers')}</div>
        </div>
        <div>
          <div class="text-3xl sm:text-4xl font-bold text-green-600">50+</div>
          <div class="text-sm text-gray-500 mt-1">{t(locale, 'home.stats.models')}</div>
        </div>
        <div>
          <div class="text-3xl sm:text-4xl font-bold text-indigo-600">{t(locale, 'home.stats.updateFrequency')}</div>
          <div class="text-sm text-gray-500 mt-1">Updates</div>
        </div>
      </div>
    </div>
  </section>

  <!-- AdSense Position 1: After Hero -->
  <section class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-6">
    <div class="adsense-placeholder h-28 rounded-lg"></div>
  </section>

  <!-- Domestic Providers Section -->
  <section id="providers" class="py-12 bg-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex items-center gap-3 mb-8">
        <span class="text-2xl">🇨🇳</span>
        <h2 class="text-2xl font-bold text-gray-900">{t(locale, 'home.domestic.title')}</h2>
        <span class="badge badge-success text-xs">{t(locale, 'home.domestic.badge')}</span>
      </div>
      
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-4">
        {domesticProviders.map((provider) => (
          <a href={`/zh/providers/${provider.id}`} class="provider-card group">
            <div class="flex items-center justify-between">
              <h3 class="font-bold text-gray-900 group-hover:text-blue-600 transition-colors">
                {provider.nameCn}
              </h3>
              <span class={`badge text-xs ${provider.overallRank <= 3 ? 'badge-success' : 'bg-gray-100 text-gray-600'}`}>
                #{provider.overallRank}
              </span>
            </div>
            <p class="text-sm text-gray-500">{provider.name}</p>
            <div class="mt-3 space-y-1 text-sm">
              <div class="flex justify-between">
                <span class="text-gray-500">{t(locale, 'common.input')}</span>
                <span class="font-medium text-gray-900">{provider.pricing.input}</span>
              </div>
              <div class="flex justify-between">
                <span class="text-gray-500">{t(locale, 'common.output')}</span>
                <span class="font-medium text-gray-900">{provider.pricing.output}</span>
              </div>
            </div>
            <div class="mt-3 pt-3 border-t border-gray-100">
              <div class="flex items-center gap-2">
                <span class="text-green-600 text-xs font-medium">{provider.availabilityCN}</span>
                <span class="flex-1"></span>
                <span class="text-xs text-gray-400">{t(locale, 'common.freeTier')}</span>
              </div>
              <p class="text-xs text-gray-500 mt-1">{provider.freeTier}</p>
            </div>
          </a>
        ))}
      </div>
    </div>
  </section>

  <!-- International Providers Section -->
  <section class="py-12 bg-gray-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex items-center gap-3 mb-8">
        <span class="text-2xl">🌍</span>
        <h2 class="text-2xl font-bold text-gray-900">{t(locale, 'home.intl.title')}</h2>
        <span class="badge badge-warning text-xs">{t(locale, 'home.intl.badge')}</span>
      </div>
      
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-4">
        {intlProviders.map((provider) => (
          <a href={`/zh/providers/${provider.id}`} class="provider-card group">
            <div class="flex items-center justify-between">
              <h3 class="font-bold text-gray-900 group-hover:text-blue-600 transition-colors">
                {provider.nameCn}
              </h3>
              <span class="badge bg-blue-100 text-blue-700 text-xs">
                #{provider.overallRank}
              </span>
            </div>
            <p class="text-sm text-gray-500">{provider.name}</p>
            <div class="mt-3 space-y-1 text-sm">
              <div class="flex justify-between">
                <span class="text-gray-500">{t(locale, 'common.input')}</span>
                <span class="font-medium text-gray-900">{provider.pricing.input}</span>
              </div>
              <div class="flex justify-between">
                <span class="text-gray-500">{t(locale, 'common.output')}</span>
                <span class="font-medium text-gray-900">{provider.pricing.output}</span>
              </div>
            </div>
            <div class="mt-3 pt-3 border-t border-gray-100">
              <div class="flex items-center gap-2">
                <span class={`text-xs font-medium ${provider.availabilityCN.includes('❌') ? 'text-red-500' : 'text-yellow-500'}`}>
                  {provider.availabilityCN}
                </span>
                <span class="flex-1"></span>
                <span class="text-xs text-gray-400">{t(locale, 'common.freeTier')}</span>
              </div>
              <p class="text-xs text-gray-500 mt-1">{provider.freeTier}</p>
            </div>
          </a>
        ))}
      </div>
    </div>
  </section>

  <!-- AdSense Position 2: After Provider Grid -->
  <section class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-6">
    <div class="adsense-placeholder h-28 rounded-lg"></div>
  </section>

  <!-- AI API 网关与聚合器 -->
  <section class="py-12 bg-indigo-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex items-center gap-3 mb-2">
        <span class="text-2xl">🔗</span>
        <h2 class="text-2xl font-bold text-gray-900">AI API 网关与聚合器</h2>
      </div>
      <p class="text-gray-500 text-sm mb-8">一个 API Key → 400+ 模型。统一访问、自动路由、灵活切换模型。</p>

      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-4">
        {aggregatorProviders.map((provider) => (
          <a href={`/zh/providers/${provider.id}`} class="provider-card group bg-white border-indigo-100 hover:border-indigo-300">
            <div class="flex items-center justify-between">
              <h3 class="font-bold text-gray-900 group-hover:text-indigo-600 transition-colors">
                {provider.nameCn}
              </h3>
              <span class="badge bg-indigo-100 text-indigo-700 text-xs">
                #{provider.overallRank}
              </span>
            </div>
            <p class="text-sm text-gray-500 mt-1">{provider.modelCount}+ 模型</p>
            <div class="mt-3 space-y-1 text-sm">
              <div class="flex justify-between">
                <span class="text-gray-500">核心优势</span>
              </div>
              <p class="text-gray-700 text-xs">{provider.highlights?.[0]}</p>
            </div>
            <div class="mt-3 pt-3 border-t border-gray-100">
              <div class="flex items-center gap-2">
                <span class="text-xs font-medium text-indigo-600">{provider.availabilityCN}</span>
                <span class="flex-1"></span>
                <span class="text-xs text-gray-400">免费额度</span>
              </div>
              <p class="text-xs text-gray-500 mt-1">{provider.freeTier}</p>
            </div>
          </a>
        ))}
      </div>

      <div class="mt-6 text-center">
        <a href="/zh/tutorials/openrouter-review" class="inline-flex items-center gap-2 text-indigo-600 hover:text-indigo-800 text-sm font-medium">
          OpenRouter 评测：一个 API Key 用 400+ 模型，值得吗？ →
        </a>
      </div>
    </div>
  </section>

  <!-- Price Comparison Table -->
  <section class="py-12 bg-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <h2 class="text-2xl font-bold text-gray-900 mb-6">{t(locale, 'home.table.title')}</h2>
      
      <div class="overflow-x-auto">
        <table class="table-token">
          <thead>
            <tr>
              <th>{t(locale, 'home.tableCols.vendor')}</th>
              <th>{t(locale, 'home.tableCols.models')}</th>
              <th>{t(locale, 'home.tableCols.input')}</th>
              <th>{t(locale, 'home.tableCols.output')}</th>
              <th>{t(locale, 'home.tableCols.cnAvailable')}</th>
              <th>{t(locale, 'home.tableCols.freeTier')}</th>
              <th></th>
            </tr>
          </thead>
          <tbody>
            {sortedProviders.map((p) => (
              <tr>
                <td class="font-semibold text-gray-900">{p.nameCn}</td>
                <td class="text-gray-600">{p.models.slice(0, 2).join(', ')}</td>
                <td class="font-mono text-sm">{p.pricing.input}</td>
                <td class="font-mono text-sm">{p.pricing.output}</td>
                <td>
                  <span class={`badge text-xs ${p.availabilityCN.includes('✅') ? 'badge-success' : p.availabilityCN.includes('⚠️') ? 'badge-warning' : 'badge-danger'}`}>
                    {p.availabilityCN}
                  </span>
                </td>
                <td class="text-xs text-gray-500">{p.freeTier}</td>
                <td>
                  <a href={`/zh/providers/${p.id}`} class="text-blue-600 hover:text-blue-800 text-sm font-medium">
                    {t(locale, 'common.viewDetails')}
                  </a>
                </td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>
    </div>
  </section>

  <!-- FAQ Section -->
  <section class="py-12 bg-gray-50">
    <div class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8">
      <h2 class="text-2xl font-bold text-gray-900 mb-8 text-center">{t(locale, 'home.faq.title')}</h2>
      
      <div class="space-y-6">
        <details class="bg-white rounded-lg border border-gray-200 p-5 group">
          <summary class="font-semibold text-gray-900 cursor-pointer list-none flex justify-between items-center">
            {t(locale, 'home.faq.q1')}
            <span class="text-gray-400 group-open:rotate-180 transition-transform">▼</span>
          </summary>
          <p class="mt-3 text-gray-600 leading-relaxed" set:html={t(locale, 'home.faq.a1')} />
        </details>
        
        <details class="bg-white rounded-lg border border-gray-200 p-5 group">
          <summary class="font-semibold text-gray-900 cursor-pointer list-none flex justify-between items-center">
            {t(locale, 'home.faq.q2')}
            <span class="text-gray-400 group-open:rotate-180 transition-transform">▼</span>
          </summary>
          <p class="mt-3 text-gray-600 leading-relaxed" set:html={t(locale, 'home.faq.a2')} />
        </details>
        
        <details class="bg-white rounded-lg border border-gray-200 p-5 group">
          <summary class="font-semibold text-gray-900 cursor-pointer list-none flex justify-between items-center">
            {t(locale, 'home.faq.q3')}
            <span class="text-gray-400 group-open:rotate-180 transition-transform">▼</span>
          </summary>
          <p class="mt-3 text-gray-600 leading-relaxed" set:html={t(locale, 'home.faq.a3')} />
        </details>
        
        <details class="bg-white rounded-lg border border-gray-200 p-5 group">
          <summary class="font-semibold text-gray-900 cursor-pointer list-none flex justify-between items-center">
            {t(locale, 'home.faq.q4')}
            <span class="text-gray-400 group-open:rotate-180 transition-transform">▼</span>
          </summary>
          <p class="mt-3 text-gray-600 leading-relaxed" set:html={t(locale, 'home.faq.a4')} />
        </details>
      </div>
    </div>
  </section>

  <!-- CTA Section -->
    <!-- 最新教程 -->
  <section class="bg-gray-50 py-16">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex items-center justify-between mb-8">
        <div>
          <h2 class="text-2xl font-bold text-gray-900">📚 最新教程</h2>
          <p class="text-gray-600 mt-1">深度指南与免费额度对比，每周更新</p>
        </div>
        <a href="/zh/tutorials" class="text-blue-600 hover:text-blue-700 font-medium hidden sm:inline">查看全部 →</a>
      </div>
      <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
        <a href="/zh/tutorials/together-ai-api-review" class="block bg-white rounded-xl border border-gray-200 p-6 hover:border-blue-300 hover:shadow-md transition-all">
          <div class="text-3xl mb-3">🤝</div>
          <span class="badge badge-primary text-xs mb-2 inline-block">提供商评测</span>
          <h3 class="text-lg font-semibold text-gray-900 mb-2">Together AI API 评测 2026：200+ 开源模型 $0.03/M</h3>
          <p class="text-sm text-gray-600 mb-3">200+ 开源模型，$0.03/M 起步价，FlashAttention-4 推理，OpenAI 兼容 API，GPU 集群。</p>
          <span class="text-blue-600 text-sm font-medium">阅读评测 →</span>
        </a>
        <a href="/zh/tutorials/tencent-hunyuan-hy3-openrouter-free-2026" class="block bg-white rounded-xl border border-gray-200 p-6 hover:border-blue-300 hover:shadow-md transition-all">
          <div class="text-3xl mb-3">🐉</div>
          <span class="badge badge-primary text-xs mb-2 inline-block">Provider Review</span>
          <h3 class="text-lg font-bold text-gray-900 mb-2">腾讯混元 Hy3：OpenRouter 免费至 7-21</h3>
          <p class="text-gray-600 text-sm">腾讯混元 Hy3（295B MoE / 21B 激活）在 OpenRouter 上免费至 2026-07-21。已验证价格、256K 上下文、与 DeepSeek-V4 Flash 对比、上线策略。</p>
          <p class="text-xs text-gray-400 mt-3">2026-07-14 · 16 min</p>
        </a>
        <a href="/zh/tutorials/modal-serverless-gpu-api-review" class="block bg-white rounded-xl border border-gray-200 p-6 hover:border-blue-300 hover:shadow-md transition-all">
          <div class="text-3xl mb-3">⚡</div>
          <span class="badge badge-primary text-xs mb-2 inline-block">Provider Review</span>
          <h3 class="text-lg font-bold text-gray-900 mb-2">Modal API 2026：Serverless GPU 云平台</h3>
          <p class="text-gray-600 text-sm">Modal 评测：Python 原生 serverless GPU 云、按 GPU 秒计费、vLLM 一行部署、$30/月免费额度。定价对比 Baseten、Replicate、RunPod。</p>
          <p class="text-xs text-gray-400 mt-3">2026年7月13日 · 14 分钟</p>
        </a>
        <a href="/zh/tutorials/gpt-5-6-sol-vs-claude-opus-4-8-production-migration" class="block bg-white rounded-xl border border-gray-200 p-6 hover:border-blue-300 hover:shadow-md transition-all">
          <div class="text-3xl mb-3">🚀</div>
          <span class="badge badge-primary text-xs mb-2 inline-block">新闻分析</span>
          <h3 class="text-lg font-bold text-gray-900 mb-2">GPT-5.6 Sol 对比 Opus 4.8：生产迁移实操</h3>
          <p class="text-gray-600 text-sm">Ploy 把默认 agent 从 Opus 4.8 切到 GPT-5.6 Sol 后成本降 27%、构建时间快 2.2 倍。拆解 4 个工程修复 + CLIProxyAPI。</p>
          <p class="text-xs text-gray-400 mt-3">2026-07-13 · 14 分钟</p>
        </a>
        <a href="/zh/tutorials/free-ai-api-models-2026-daily-use" class="block bg-white rounded-xl border border-gray-200 p-6 hover:border-blue-300 hover:shadow-md transition-all">
          <div class="text-3xl mb-3">🆓</div>
          <span class="badge badge-primary text-xs mb-2 inline-block">入门指南</span>
          <h3 class="text-lg font-bold text-gray-900 mb-2">2026 免费 AI API：14 个平台 + 3 家日常推荐</h3>
          <p class="text-gray-600 text-sm">14 个国内外免费 AI API 平台对比，3 家日常推荐，无需信用卡。</p>
          <p class="text-xs text-gray-400 mt-3">2026-07-06 · 12 分钟</p>
        </a>
        <a href="/zh/tutorials/ai-api-free-tiers-2026" class="block bg-white rounded-xl border border-gray-200 p-6 hover:border-blue-300 hover:shadow-md transition-all">
          <div class="text-3xl mb-3">💰</div>
          <span class="badge badge-primary text-xs mb-2 inline-block">横评对比</span>
          <h3 class="text-lg font-bold text-gray-900 mb-2">2026 AI API 免费额度：26 家厂商对比</h3>
          <p class="text-gray-600 text-sm">三大梯队免费额度横评，覆盖所有主流 AI 提供商。</p>
          <p class="text-xs text-gray-400 mt-3">2026-06-04 · 14 分钟</p>
        </a>
        <a href="/zh/tutorials/cheapest-llm-api-pricing-2026" class="block bg-white rounded-xl border border-gray-200 p-6 hover:border-blue-300 hover:shadow-md transition-all">
          <div class="text-3xl mb-3">🏷️</div>
          <span class="badge badge-primary text-xs mb-2 inline-block">横评对比</span>
          <h3 class="text-lg font-bold text-gray-900 mb-2">2026 最便宜 LLM API 价格</h3>
          <p class="text-gray-600 text-sm">按每 token 经济性排序的最便宜 API 提供商。</p>
          <p class="text-xs text-gray-400 mt-3">2026-06-02 · 11 分钟</p>
        </a>
        <a href="/zh/tutorials/meta-model-api-review" class="block bg-white rounded-xl border border-gray-200 p-6 hover:border-blue-300 hover:shadow-md transition-all">
          <div class="text-3xl mb-3">🔷</div>
          <span class="badge badge-primary text-xs mb-2 inline-block">提供商评测</span>
          <h3 class="text-lg font-bold text-gray-900 mb-2">Meta Model API 2026 评测：Muse Spark 1.1 深度解析</h3>
          <p class="text-gray-600 text-sm">Meta 官方 API 全面评测：Muse Spark 1.1 定价、Agentic 工具调用、1M 上下文、搜索增强。</p>
          <p class="text-xs text-gray-400 mt-3">2026-07-12 · 15 分钟</p>
        </a>
      </div>
    </div>
  </section>

<section class="py-16 bg-blue-600">
    <div class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
      <h2 class="text-2xl sm:text-3xl font-bold text-white mb-4">
        {t(locale, 'home.cta.title')}
      </h2>
      <p class="text-blue-100 mb-8">
        {t(locale, 'home.cta.subtitle')}
      </p>
      <div class="flex flex-col sm:flex-row gap-3 justify-center">
        <a href="#providers" class="bg-white text-blue-600 font-semibold py-3 px-8 rounded-lg hover:bg-blue-50 transition-colors">
          {t(locale, 'home.cta.btnCompare')}
        </a>
        <a href="/zh/tutorials" class="bg-blue-700 text-white font-semibold py-3 px-8 rounded-lg hover:bg-blue-800 transition-colors">
          {t(locale, 'home.cta.btnTutorials')}
        </a>
      </div>
    </div>
  </section>
</BaseLayout>
