---
import BaseLayout from '../../../layouts/BaseLayout.astro';

const slug = 'openai-agents-api-gpt-live-1-voice-2026';
const date = '2026-09-11';
const locale = 'zh';

const zhTitle = "OpenAI Agents API 与 GPT-Live 1 语音 API 2026 价格";
const zhDescription = "OpenAI Agents API 评测 2026：托管 Codex harness、会话计费、容器费率，以及全双工语音 GPT-Live 1 每分钟 $0.05。数据已核对。";
const canonicalUrl = `https://apirank.vip/zh/tutorials/${slug}`;

const articleJsonLd = {
  "@context": "https://schema.org",
  "@type": "Article",
  "headline": "OpenAI Agents API 与 GPT-Live 1 语音 API 2026 价格",
  "description": "OpenAI Agents API 评测 2026：托管 Codex harness、会话计费、容器费率，以及全双工语音 GPT-Live 1 每分钟 $0.05。数据已核对。",
  "datePublished": "2026-09-11",
  "dateModified": "2026-09-11",
  "inLanguage": "zh-CN",
  "author": {
    "@type": "Organization",
    "name": "APIRank",
    "url": "https://apirank.vip"
  },
  "publisher": {
    "@type": "Organization",
    "name": "APIRank",
    "logo": {
      "@type": "ImageObject",
      "url": "https://apirank.vip/favicon.svg"
    }
  },
  "mainEntityOfPage": {
    "@type": "WebPage",
    "@id": "https://apirank.vip/zh/tutorials/openai-agents-api-gpt-live-1-voice-2026"
  }
};
const breadcrumbJsonLd = {
  "@context": "https://schema.org",
  "@type": "BreadcrumbList",
  "itemListElement": [
    {
      "@type": "ListItem",
      "position": 1,
      "name": "首页",
      "item": "https://apirank.vip"
    },
    {
      "@type": "ListItem",
      "position": 2,
      "name": "教程",
      "item": "https://apirank.vip/zh/tutorials"
    },
    {
      "@type": "ListItem",
      "position": 3,
      "name": "OpenAI Agents API 与 GPT-Live 1 语音 API 2026 价格",
      "item": "https://apirank.vip/zh/tutorials/openai-agents-api-gpt-live-1-voice-2026"
    }
  ]
};
const faqJsonLd = {
  "@context": "https://schema.org",
  "@type": "FAQPage",
  "mainEntity": [
    {
      "@type": "Question",
      "name": "OpenAI Agents API 是什么？",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "Agents API 让应用通过一个由 OpenAI 托管的 API 访问 Codex harness。OpenAI 管理会话、编排、上下文压缩与恢复，你的应用提供工具并选择执行环境。它围绕四个概念构建：Agent（模型、指令、工具、MCP server）、Environment（agent 读取文件并运行命令的可选沙箱）、Session（agent 的持久化实例），以及 Events and items（会话中的输入与输出）。它支持流式、webhook、programmatic tool calling、带 subagent 的多 agent 编排，以及 MCP server。"
      }
    },
    {
      "@type": "Question",
      "name": "OpenAI Agents API 如何计费？",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "Agents API 没有单独定价 SKU。模型用量按所选模型的 API 费率计费，OpenAI 工具按其标准费率，OpenAI 托管沙箱按标准容器费率。容器按每 20 分钟会话计费：1 GB 为 $0.03、4 GB 为 $0.12、16 GB 为 $0.48、64 GB 为 $1.92。符合条件的容器会话按分钟计费，每会话 5 分钟起。官方示例中的会话运行在 gpt-6-astra 上，每 100 万输入 token $10、缓存输入 $1、缓存写入 $12.50、输出 $50，输入超过 272,000 token 的请求按输入与缓存 2 倍、输出 1.5 倍计费。"
      }
    },
    {
      "@type": "Question",
      "name": "OpenAI Agents API 支持零数据保留（ZDR）吗？",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "不支持。OpenAI 文档写明 Agents API 目前仅支持美国境内的数据驻留，且不支持零数据保留。选择自托管沙箱并不会让 Agents API 具备 ZDR 资格，因为托管 harness 仍然是编排者。把 ZDR 当作硬性要求的组织无法使用 Agents API，无论它能替代多少编排代码。"
      }
    },
    {
      "@type": "Question",
      "name": "GPT-Live 1 是什么？",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "GPT-Live 1（模型 ID 为 gpt-live-1）是 OpenAI 的全双工语音模型，用于实时对话。全双工意味着它能够同时听说，并把推理与工具调用委派给后端 agent。这种切分让你可以独立于语音模型来选择后端模型或 agent，区别于由单一模型同时处理语音、推理与工具选择的 Realtime API。打断说话不会自动取消后端工作。其知识截止日期为 2025 年 7 月 31 日，支持音频与文本输入输出。"
      }
    },
    {
      "@type": "Question",
      "name": "GPT-Live 1 每分钟多少钱？",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "GPT-Live 1 语音会话为每分钟 $0.05，按秒计费，且会话时长不会向上取整到整分钟。后端模型与工具用量按你所配置模型或 agent 的标准费率单独计费。相关语音模型价格：gpt-live-transcribe 每分钟 $0.017、gpt-realtime-translate 每分钟 $0.034、gpt-realtime-whisper 每分钟 $0.017。速率限制按并发会话数衡量：Tier 1 为 25、Tier 2 为 50、Tier 3 为 200、Tier 4 为 300、Tier 5 为 500。免费档不支持。"
      }
    },
    {
      "@type": "Question",
      "name": "可以在 Cloudflare Containers 上运行 OpenAI Agents API 吗？",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "可以。Cloudflare 2026 年 9 月 10 日的 changelog 记录 Cloudflare Containers 可以为 OpenAI Agents API 提供自托管执行环境，并以一个开源 Workers 模板作为参考实现。该 Worker 为每个 Codex 会话维持一个 Cloudflare Container，保持进行中的工作持续运行，在后续输入时重连，并在闲置时自动关闭。这把 agent 账单中的容器一条从 OpenAI 每 20 分钟费率转移到你已付费的基础设施上，同时保留托管 harness。它不会让会话具备 ZDR 资格。"
      }
    }
  ]
};

const bodyContent = `
<article class="prose prose-lg max-w-none">
  <h2 id="what-changed">同一周内的两次发布，指向两个相反的问题</h2>
  <p>OpenAI 的 API 能力面在同一周内动了两次，而这两项改动值得放在一起读，因为它们解决的是正好相反的问题。<strong>Agents API</strong> 回答的是"我怎样才能不再自己写 agent 循环"——OpenAI 现在托管一个 Codex harness，保存会话状态，然后把一个持久化 session 对象交给你的应用，而不是一段需要你自己重新拼装的 transcript。<strong>GPT-Live 1</strong> 回答的是另一个问题："我怎样才能让用户在语音助手说到一半时打断它？"它是一个全双工语音模型，边说边听，并把真正的推理交给后端 agent。</p>

  <p>对集成团队来说，这两块正好是当下技术栈里最贵的胶水。编排与语音，是团队花几周去重建厂商本可以托管的基础设施的地方。对一个比价站而言，重要的不是发布稿，而是各自的单位成本、它强迫你放弃什么，以及它会悄悄让哪些现有技术栈过时。本文依据厂商官方文档把两者都拆开讲。</p>

  <h2 id="agents-api">Agents API：以会话语义暴露的托管 Codex Harness</h2>
  <p>Agents API 让应用通过一个 <strong>由 OpenAI 托管的 API 访问 Codex harness</strong>。OpenAI 管理会话、编排、上下文压缩与恢复；你的应用提供工具并选择执行环境。文档构建一切概念的四个基础是：<strong>Agent</strong>（模型、指令、工具与 MCP server）、<strong>Environment</strong>（agent 读取文件、加载技能、运行命令的可选沙箱或计算机）、<strong>Session</strong>（agent 的持久化实例，负责执行任务并响应输入），以及 <strong>Events and items</strong>（一次会话中发送的输入与产生的输出）。</p>

  <p>整个循环被刻意做得很短。你创建 session 并配置 agent，OpenAI 负责把环境准备好。你给它一个任务，环境就绪后即开始一轮工作。你通过流式输出或 webhook 跟踪进度，得知 agent 完成或需要输入。然后你要么继续同一个 session，要么在它执行途中进行干预。在托管模式下，你的应用只负责发送输入、接收事件。</p>

  <p>托管 harness 真正提供的东西，恰恰是过去属于你代码库的那部分：在沙箱中运行命令与代码、应用技能与指令、通过工具或 MCP 连接外部数据、在 agent 工作时进行干预、<strong>对已完成工作进行摘要以管理上下文窗口</strong>、把工作拆成子任务并委派给 subagent，以及把 session 从中断处恢复。如果你曾经眼看着一个长时间运行的 agent 因为 transcript 超出模型上限而崩掉，那么"摘要"这一步就是最值得关注的地方。</p>

  <h3 id="agents-api-runtimes">它只是三种运行时之一，并不取代其余两种</h3>
  <p>文档里最有价值的内容是一张大多数发布报道都会跳过的对比表。OpenAI 现在提供三种运行 agent 的方式，它们之间并不互换：</p>

  <table>
    <thead><tr><th>运行时</th><th>谁运行循环</th><th>任务之间的状态</th><th>集成工作量</th><th>执行环境</th></tr></thead>
    <tbody>
      <tr><td><strong>Agents API</strong></td><td>OpenAI 运行托管的 Codex harness</td><td>保存的会话配置、轮次与条目</td><td>低</td><td>OpenAI 托管沙箱、自托管沙箱，或无沙箱</td></tr>
      <tr><td>Agents SDK</td><td>你的应用；runner 处理循环与 handoff</td><td>你自己的存储与 SDK 会话，或 Responses 会话状态</td><td>中</td><td>你的运行时与沙箱服务商集成</td></tr>
      <tr><td>Responses API</td><td>你的应用，可选托管编排</td><td>手动历史、response 链式拼接，或 Conversations</td><td>高</td><td>你自己的执行环境</td></tr>
    </tbody>
  </table>

  <p>实用的读法：如果你已经用 <strong>Agents SDK</strong> 构建了 agent 循环且运行良好，那么迁移到 Agents API 换来的是托管上下文压缩与恢复，代价是失去对部署、存储、审批与运行时集成的控制——而这四项恰恰是 SDK 存在的意义。如果你还在裸用 <strong>Responses API</strong> 手工维护历史，那么 Agents API 是更大的收益，因为会话持久化正是你目前一直在自己伪造的东西。</p>

  <h3 id="agents-api-pricing">Agents API 的成本结构</h3>
  <p>Agents API 没有单独定价 SKU，而这一点最容易让成本预测出错。模型用量按<strong>所选模型的 API 费率</strong>计费；OpenAI 工具按其标准费率；OpenAI 托管沙箱按标准容器费率。因此一次 agent 会话的成本拆成三条线——token、工具、容器时间——而只有第一条是你已经熟悉的模型价格。</p>

  <p>上限来自容器，因为长时间运行的 agent 会在整个会话期间占住一个容器。托管的 Shell 与 Code Interpreter 容器按每 20 分钟会话、每容器计费：</p>

  <table>
    <thead><tr><th>容器内存</th><th>每 20 分钟会话</th><th>折合每小时 agent 时间的上限</th></tr></thead>
    <tbody>
      <tr><td>1 GB</td><td>$0.03</td><td>$0.09</td></tr>
      <tr><td>4 GB</td><td>$0.12</td><td>$0.36</td></tr>
      <tr><td>16 GB</td><td>$0.48</td><td>$1.44</td></tr>
      <tr><td>64 GB</td><td>$1.92</td><td>$5.76</td></tr>
    </tbody>
  </table>

  <p>做预测时有两个脚注很重要。符合条件的容器会话<strong>按分钟计费，每个会话 5 分钟起</strong>，因此一批极短的 agent 运行会按每次五分钟计价，而不是按真实时长。另外这里的 GB 指二进制吉字节——1 GB 等于 2^30 字节——在你拿 64 GB 容器去装 60 GB 工作负载、却疑惑为什么装不下之前，这个区分无关紧要。</p>

  <p>把模型叠上去，算术就变得具体了。厂商自己的示例中会话运行在 <code>gpt-6-astra</code> 上，价格为 <strong>每 100 万输入 token $10、每 100 万缓存输入 $1、每 100 万缓存写入 $12.50</strong>，以及 <strong>每 100 万输出 token $50</strong>。其上还有两条定价规则：输入超过 <strong>272,000 token</strong> 的请求，整个请求按输入与缓存费率的 2 倍、输出费率的 1.5 倍计费；batch 与 flex 模式按标准价的 50%，fast 模式按 2 倍。对一个先把仓库读一遍、然后反复迭代的编码 agent 来说，缓存输入那一条才是钱真正流向的地方——这也解释了为什么缓存写入要单独按未缓存输入费率的 1.25 倍计价。</p>

  <h3 id="agents-api-zdr">开始原型开发之前应当读到的合规限制</h3>
  <p>文档把这一点写得很直白，而它恰恰是那种只有在你围绕该 API 完成架构之后才会显现的约束：<strong>Agents API 目前仅支持美国境内的数据驻留，并且不支持零数据保留（ZDR）</strong>。选择自托管沙箱并不会让 Agents API 具备 ZDR 资格。如果你的组织把 ZDR 当作硬性要求，那么无论它能替代多少编排代码，Agents API 都不在候选之列——因为托管 harness 本身就是产品，自托管沙箱并不能移动数据边界。</p>

  <h2 id="gpt-live-1">GPT-Live 1：全双工语音，外加一个真正干活的后端</h2>
  <p>GPT-Live 1 是 OpenAI 面向自然、富有表现力的语音对话、并能平滑处理打断的"旗舰模型"。模型 ID 为 <code>gpt-live-1</code>，其决定性能力是<strong>全双工</strong>：能够同时听说。整体架构把工作切成两半。GPT-Live 负责对话——听、说，并判断何时向后端求助。后端负责被委派的任务——推理、调用工具，并把结果返回给 GPT-Live 去表达。</p>

  <p>这个切分才是最值得理解的设计决定，因为它正是 GPT-Live 与 Realtime API 的分野。用 Realtime，一个模型同时承担语音、推理与工具选择。用 GPT-Live，<strong>你可以独立于语音模型来挑选后端模型或 agent</strong>。文档把取舍讲得很直接：把详细指令、业务规则与工具工作流放在后端；把对话风格与委派触发条件放在给 GPT-Live 的简短提示里。你的应用依然负责权限、确认、私有函数执行与持久化任务状态。</p>

  <p>有一个会对产品行为产生实际影响的细节：<strong>打断说话不会自动取消后端工作</strong>。后端任务运行期间对话可以继续进行，这正是"用户补一句细节、同时后端去查订单状态"显得自然而非故障的原因——也正是你在设计取消逻辑时必须考虑的事，因为让用户停止说话，并不等于让背后的任务停止。</p>

  <h3 id="gpt-live-pricing">GPT-Live 1 定价：按秒，而非按 token</h3>
  <p>语音按时长定价，这会改变你的建模方式。GPT-Live 1 会话为 <strong>每分钟 $0.05，按秒计费</strong>，且会话时长<em>不会</em>向上取整到整分钟。后端模型与工具用量按你所配置对象的标准费率单独计费。最后这句是必须内化的：$0.05 只买语音层。后端执行的每一次委派，都是第二张账单。</p>

  <table>
    <thead><tr><th>组件</th><th>价格</th><th>计费单位</th></tr></thead>
    <tbody>
      <tr><td><code>gpt-live-1</code> 语音会话</td><td>$0.05</td><td>分钟，按秒，不向上取整</td></tr>
      <tr><td>后端（Responses 或 client 委派）</td><td>所配置模型的正常费率</td><td>token</td></tr>
      <tr><td><code>gpt-live-transcribe</code></td><td>$0.017</td><td>分钟</td></tr>
      <tr><td><code>gpt-realtime-translate</code></td><td>$0.034</td><td>分钟</td></tr>
      <tr><td><code>gpt-realtime-whisper</code></td><td>$0.017</td><td>分钟</td></tr>
    </tbody>
  </table>

  <p>两种委派模式是决定成本结构的关键。用 <strong>Responses 委派</strong>，GPT-Live 调用一个受支持的 Responses 模型，提供对话上下文，并返回结果——托管省心，但两半都由你向 OpenAI 付费，且你的上下文会流出去。用 <strong>client 委派</strong>，由你的应用控制后端执行、上下文，以及哪些结果能到达 GPT-Live——这也是接入非 OpenAI 模型、或你已在运行的 agent harness 的方式。模式在创建 session 时选定；要更换就得开新 session。</p>

  <h3 id="gpt-live-limits">速率限制与端点形态</h3>
  <p>语音会话按<strong>并发数而非每分钟请求数</strong>限流，且 GPT-Live 1 不支持免费档：</p>

  <table>
    <thead><tr><th>档位</th><th>并发会话数</th></tr></thead>
    <tbody>
      <tr><td>Tier 1</td><td>25</td></tr>
      <tr><td>Tier 2</td><td>50</td></tr>
      <tr><td>Tier 3</td><td>200</td></tr>
      <tr><td>Tier 4</td><td>300</td></tr>
      <tr><td>Tier 5</td><td>500</td></tr>
    </tbody>
  </table>

  <p>端点表是团队最容易踩坑的地方。GPT-Live 1 <strong>只</strong>支持 <code>v1/live/sessions</code>。Chat Completions、Responses、Realtime、实时转写与实时翻译端点、Assistants、Batch、微调、Embeddings、图像与语音端点——全部标记为不支持。支持的特性是流式与函数调用；结构化输出、微调与预测输出明确不支持。这是一个专用端点，而不是把新模型塞进旧 Realtime 路径，任何假定可以把现有 Realtime 客户端指向 <code>gpt-live-1</code> 的集成方案都需要返工。</p>

  <p>输入模态为音频与文本，输出为音频与文本；图像与视频不支持，知识截止日期为 2025 年 7 月 31 日。连接方式覆盖了真实的部署形态：面向浏览器语音应用的 WebRTC（媒体轨传输音频、数据通道传输 JSON 事件）、面向服务端音频集成的 WebSockets、面向后端接入既有会话的 sideband 连接，以及电话接入的 SIP／电信指导。另外还有 LiveKit、Twilio、Telnyx 与 Daily/Pipecat 的官方合作路径——如果你的媒体管道已经存在、不希望替换，这点很重要。</p>

  <h2 id="cloudflare">两者在 Cloudflare Containers 上的交汇</h2>
  <p>连接这两项发布的桥梁来自 Cloudflare 自家 changelog，日期为 2026 年 9 月 10 日。Agents API 的 <strong>environment</strong> 概念允许你提供自托管执行环境，而不必使用 OpenAI 托管沙箱，而 Cloudflare Containers 现在可以充当其中之一，并以一个开源 Workers 模板作为参考实现。该 Worker 为每个 Codex 会话维持一个 Cloudflare Container，保持进行中的工作持续运行，在后续输入时重连，并在闲置时自动关闭。</p>

  <p>经济逻辑是这部分值得单列一节而非一句脚注的原因。自托管执行环境把 agent 账单中的容器这一条，从 OpenAI 的每 20 分钟费率转移到你已经付费的基础设施上，同时保留你最初选择 Agents API 所要的托管 harness——会话、编排、上下文压缩、恢复。注意边界：自托管沙箱并<strong>不</strong>会让会话具备 ZDR 资格，因为管理 harness 仍然是编排者。它改变的是代码在哪里执行、容器花多少钱，而不是会话状态存放在哪里。</p>

  <h2 id="migration">迁移决策，直说</h2>
  <p><strong>如果你在 Realtime API 上跑语音产品</strong>，问题在于独立的后端推理是否值得换一个新端点。GPT-Live 的价值在于"说话的那个模型不是思考的那个模型"，因此后端可以是更强的模型、更便宜的模型，或一个你已经信任的 agent harness。如果你的语音 agent 只是单模型的轮流对话体验，Realtime 依然合适，迁移未必划算。</p>

  <p><strong>如果你为长时间运行的 agent 手工维护上下文</strong>，Agents API 能砍掉真实的代码量，同时也会砍掉控制权：除非走自托管环境路线，否则部署、存储与审批都会移到厂商侧。诚实的说法是，你是在用一套代码库换一个依赖，而这个交易只有在 harness 的那些特性——压缩、subagent 委派、恢复——是你本来也会做得很糟的东西时才划算。</p>

  <p><strong>如果合规要求 ZDR 或非美国驻留</strong>，到此为止。托管与自托管沙箱两种形态都不让 Agents API 具备 ZDR 资格，且目前驻留仅限美国。这是文档写明的约束，而不是可以在集成层绕过的限制。</p>

  <h2 id="verdict">结论</h2>
  <p>这两项发布回答的，都是过去由你自己的代码来回答的问题。对多数团队而言，Agents API 影响更大，因为 agent 编排胶水是纯粹的成本中心——没有客户会注意到你的上下文压缩器，他们只会注意到它失败的时候。它的定价结构奖励理解它的人：会话按 token 加工具加容器分钟计费，容器计时有 5 分钟下限，输入超过 272K token 的部分入库时按双倍计费。把这三条线建模正确的团队能预测账单；用 token 费率乘以 token 数的团队会被吓到。</p>

  <p>GPT-Live 1 则是范围更窄但更干净的胜利。全双工加独立后端是语音的正确架构，端点形态清楚地说明了它不做什么，而每分钟 $0.05 按秒计费是可以直接写进表格、无需猜测的费率。需要注意的只是它仅为语音层定价——后端对话才是你真正要预测的变量，而这正是比价站的模型表发挥价值的地方。</p>

  <p>这周这一对之所以值得注意，不是因为任一功能本身，而是它指向的方向：OpenAI 正在把 harness 与语音层产品化，这意味着集成团队剩下的差异化在向栈上层移动。接下来两个季度能赢的团队，是那些清楚知道自己还在手工维护 agent 栈中哪些部分的团队。</p>

  <p class="text-sm text-gray-500 italic">本文数据经 OpenAI 开发者文档（Agents API、GPT-Live 1、GPT-Live-Transcribe 与 GPT-6 Astra）、OpenAI 定价页，以及 Cloudflare 2026 年 9 月 10 日 changelog，于 2026 年 9 月 11 日核对。openai.com 营销页对自动抓取返回 403；本文所有数字均来自 developers.openai.com 文档，该站点可直接返回 markdown 版本。</p>
</article>
`;
---

<BaseLayout title={zhTitle + ' | APIRank'} description={zhDescription} canonicalUrl={canonicalUrl} lang="zh">
  <script type="application/ld+json" set:html={JSON.stringify(articleJsonLd)} />
  <script type="application/ld+json" set:html={JSON.stringify(breadcrumbJsonLd)} />
  <script type="application/ld+json" set:html={JSON.stringify(faqJsonLd)} />

  <div class="container mx-auto px-4 py-8 max-w-5xl">
    <header class="mb-8">
      <div class="flex items-center gap-2 text-sm text-gray-500 mb-3">
        <span class="bg-blue-100 text-blue-800 px-2 py-1 rounded">商家评测</span>
        <time datetime="2026-09-11">2026 年 9 月 11 日</time>
        <span>·</span>
        <span>阅读约 14 分钟</span>
      </div>
      <h1 class="text-4xl font-bold text-gray-900 mb-4">{zhTitle}</h1>
      <p class="text-xl text-gray-600">{zhDescription}</p>
    </header>

    <div class="grid lg:grid-cols-[1fr_300px] gap-8">
      <div>
        <article class="prose prose-lg max-w-none" set:html={bodyContent} />

        <div class="bg-blue-50 border border-blue-200 rounded-xl p-6 mt-12">
          <h3 class="font-bold text-gray-900 mb-2">试用 OpenAI Agents API 与 GPT-Live 1</h3>
          <p class="text-gray-600 text-sm mb-4">Agents API 会话按 token 加工具加容器分钟计费；GPT-Live 1 语音为每分钟 $0.05 按秒计费。做成本预测前请先核对官方定价页。</p>
          <a href="https://developers.openai.com/api/docs/pricing" target="_blank" rel="noopener" class="inline-block bg-blue-600 text-white px-6 py-2 rounded-lg font-medium hover:bg-blue-700 transition-colors">查看 OpenAI 定价 →</a>
        </div>
      </div>

      <aside class="space-y-6">
        <div class="bg-white rounded-xl border border-gray-200 p-6">
          <h3 class="font-bold text-gray-900 mb-4">关键事实</h3>
          <dl class="space-y-3 text-sm">
            <div><dt class="text-gray-500">厂商</dt><dd class="font-medium">OpenAI</dd></div>
            <div><dt class="text-gray-500">Agents API</dt><dd class="font-medium">托管 Codex harness</dd></div>
            <div><dt class="text-gray-500">会话计费</dt><dd class="font-medium">模型 + 工具 + 容器</dd></div>
            <div><dt class="text-gray-500">容器 4 GB</dt><dd class="font-medium">$0.12 / 20 分钟会话</dd></div>
            <div><dt class="text-gray-500">容器下限</dt><dd class="font-medium">5 分钟起</dd></div>
            <div><dt class="text-gray-500">GPT-Live 1</dt><dd class="font-medium">全双工语音</dd></div>
            <div><dt class="text-gray-500">语音价格</dt><dd class="font-medium text-emerald-600">$0.05 / 分钟</dd></div>
            <div><dt class="text-gray-500">语音端点</dt><dd class="font-medium">仅 v1/live/sessions</dd></div>
            <div><dt class="text-gray-500">语音并发</dt><dd class="font-medium">按档位 25 至 500</dd></div>
            <div><dt class="text-gray-500">数据驻留</dt><dd class="font-medium text-amber-600">仅美国，不支持 ZDR</dd></div>
          </dl>
          <div class="mt-4 pt-4 border-t border-gray-100 text-xs text-gray-400">
            <p>价格更新于 2026/9/11</p>
            <p class="mt-1">请以官网最新定价为准</p>
          </div>
        </div>

        <div class="bg-white rounded-xl border border-gray-200 p-6">
          <h3 class="font-bold text-gray-900 mb-4">相关商家</h3>
          <div class="space-y-2">
            <a href="/zh/providers/openai" class="flex items-center justify-between py-2 text-sm hover:text-blue-600 transition-colors">
              <span>OpenAI</span><span class="text-gray-400">→</span>
            </a>
            <a href="/zh/providers/cloudflare-workers-ai" class="flex items-center justify-between py-2 text-sm hover:text-blue-600 transition-colors">
              <span>Cloudflare Workers AI</span><span class="text-gray-400">→</span>
            </a>
            <a href="/zh/providers/cloudflare-ai-gateway" class="flex items-center justify-between py-2 text-sm hover:text-blue-600 transition-colors">
              <span>Cloudflare AI Gateway</span><span class="text-gray-400">→</span>
            </a>
            <a href="/zh/providers/elevenlabs" class="flex items-center justify-between py-2 text-sm hover:text-blue-600 transition-colors">
              <span>ElevenLabs</span><span class="text-gray-400">→</span>
            </a>
            <a href="/zh/providers" class="block text-center text-blue-600 text-sm font-medium py-2 hover:underline">查看全部 →</a>
          </div>
        </div>

        <div class="bg-white rounded-xl border border-gray-200 p-6">
          <h3 class="font-bold text-gray-900 mb-4">相关文章</h3>
          <div class="space-y-2 text-sm">
            <a href="/zh/tutorials/openai-cursor-api-cutoff-november-12-2026" class="block py-1 hover:text-blue-600 transition-colors">OpenAI 切断 Cursor API：迁移路线</a>
            <a href="/zh/tutorials/gpt-6-astra-api-pricing-2026" class="block py-1 hover:text-blue-600 transition-colors">GPT-6 Astra API 定价 2026</a>
            <a href="/zh/tutorials/gpt-5-6-sol-sandbox-design-agent-api-2026" class="block py-1 hover:text-blue-600 transition-colors">GPT-5.6 Sol 沙箱设计 Agent API</a>
            <a href="/zh/tutorials/cloudflare-workers-ai-china-models-2026" class="block py-1 hover:text-blue-600 transition-colors">Cloudflare Workers AI：中国模型</a>
          </div>
        </div>
      </aside>
    </div>
  </div>
</BaseLayout>
